#include "main.h"
#include <ctype.h>

/**
 * string_toupper - Entry point
 * function that changes all lowercase letters of a string to uppercase
 * @ch: string to be changed to uppercase
 * Return: on Success - string
 */
char *string_toupper(char *ch)
{
	ch = toupper('*');
	return (ch);
}
